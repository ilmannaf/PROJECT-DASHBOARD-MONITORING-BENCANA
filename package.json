{
  "name": "dashboard-monitoring-bencana",
  "version": "1.0.0",
  "description": "Dashboard Monitoring Bencana BPBD Kota Semarang",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "dev": "cd backend && nodemon src/server.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
